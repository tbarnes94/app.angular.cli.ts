/** @imports */
import { Translations as Modules } from './modules';
import { Translations as Section } from './section';

/** @exports */
export const Translations: any = {
  modules: Modules,
  section: Section,
};
